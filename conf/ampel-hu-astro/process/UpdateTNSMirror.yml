name: UpdateTNSMirror
tier: null
schedule: every(5).minutes
processor:
  unit: OpsProcessor
  config:
    execute:
      unit: TNSMirrorUpdater
      config:
        api_key:
          label: tns/complete
